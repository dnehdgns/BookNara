<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="image">

    <insert id="insertEventImage" parameterType="map">
        INSERT INTO IMAGES (TARGET_TYPE, TARGET_ID, IMG_TYPE, IMG_URL)
        VALUES ('EVENT', #{eventId}, #{imgType}, #{imgUrl})
    </insert>

    <!-- (이미 있으면 유지) -->
    <select id="selectAll" resultType="com.booknara.booknaraPrj.events.dto.ImageDTO">
        SELECT * FROM IMAGES ORDER BY IMAGE_ID DESC
    </select>

</mapper>
