<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.booknara.booknaraPrj.bookMark.mapper.BookmarkMapper">

    <select id="selectBookmarkYn" resultType="string">
        SELECT BOOKMARK_YN
        FROM BOOKMARK
        WHERE ISBN13 = #{isbn13}
          AND USER_ID = #{userId}
    </select>

    <insert id="upsertBookmark">
        INSERT INTO BOOKMARK (
            ISBN13,
            USER_ID,
            BOOKMARK_YN
        )
        VALUES (
                   #{isbn13},
                   #{userId},
                   'Y'
               )
            ON DUPLICATE KEY UPDATE
                                 BOOKMARK_YN = 'Y',
                                 UPDATED_AT  = CURRENT_TIMESTAMP
    </insert>

    <update id="cancelBookmark">
        UPDATE BOOKMARK
        SET
            BOOKMARK_YN = 'N',
            UPDATED_AT  = CURRENT_TIMESTAMP
        WHERE ISBN13 = #{isbn13}
          AND USER_ID = #{userId}
    </update>

    <select id="countBookmarkByIsbn" resultType="int">
        SELECT COUNT(*)
        FROM BOOKMARK
        WHERE ISBN13 = #{isbn13}
          AND BOOKMARK_YN = 'Y'
    </select>

</mapper>